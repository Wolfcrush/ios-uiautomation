describe("Banjo - Change Screen Orientation (Landscape).", function() {

	it("SCREEN.LANDSCAPE", function() {

	target.setDeviceOrientation(UIA_INTERFACE_ORIENTATION_LANDSCAPERIGHT);
	target.delay(1);

	});
});